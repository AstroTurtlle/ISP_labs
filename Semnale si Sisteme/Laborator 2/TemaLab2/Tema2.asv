n = -5:0.1:5;
x = @(n)(4.*cos(pi*n));
subplot(2,3,1);
stem(n,x(n));

n = -5:0.1:5;
x = @(n)(2.*sin(3*n));
subplot(2,3,2);
stem(n,x(n));

n = -5:0.1:5;
x = @(n)(0.9.^n.*cos(pi*n/10));
subplot(2,3,3);
stem(n,x(n));

t = -5:0.1:5;
x = @(n)(4.*cos(pi*n));
subplot(2,3,1);
stem(n,x(n));

n = -5:0.1:5;
x = @(n)(4.*cos(pi*n));
subplot(2,3,1);
stem(n,x(n));

n = -5:0.1:5;
x = @(n)(4.*cos(pi*n));
subplot(2,3,1);
stem(n,x(n));
